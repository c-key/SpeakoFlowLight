# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

package-lock.json

node_modules
dist
dist-ssr
*.local
*.local.*

# Local Tauri build override (per-machine; skips code signing / updater
# artifacts for local `tauri build` runs). Not for the shipped/CI build.
tauri.local-build.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.claude/

/target/
recording_*
.crush/

# Local cargo config (per-machine build/linker settings; not needed to build the app)
.cargo/

.direnv
.envrc

# Nix build output
result

# Knowledge-graph analysis output: internal AI-tooling artifact, kept out of
# the public repo entirely (regenerable from source).
graphify-out/

# Editor / agent tooling
.kiro/
.agents/
.impeccable/

# Firecrawl MCP diagnostic scratch files (may contain API keys)
.fc_*

# Secrets / credentials — never commit real keys. The app already stores keys
# in the OS keychain (secret_store.rs) and its working settings live under the
# OS app-data dir, so none of this is normally in-tree; these rules are a guard
# against accidental drops.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.keystore
credentials.json
secrets.json
# Local Tauri app settings store (holds provider API keys when configured).
settings_store.json

# Internal marketing/testing website — kept out of the app repo
speakoflow-website/

# Internal generated docs/reference HTML (local-only, regenerable)
speakoflow-docs.html
speakoflow-feature-reference.html

# Internal launch & competitive-strategy playbook — kept out of the public repo
# (self-declared internal; contains launch tactics + competitor analysis).
LAUNCH_PLAN.md

# Maintainer's working notes — plans, session prompts, progress logs, release
# checklists, provider audits, prompt references, redesign write-ups. These are
# local scratch documents, not product documentation, so the whole folder stays
# out of the public repo. User-facing docs live in README.md, BUILD.md,
# CONTRIBUTING.md, PRIVACY.md and on speakoflow.com/docs.
docs/

# Demo / marketing capture tooling (not shipped, regenerable, dev-only).
# The capture harness, its config, the demo-only entry points, and the
# generated screenshots are all kept out of the public repo.
demo-assets/
demo-capture/
src/assistant/panel-demo.html
src/assistant/panel-demo.tsx
src/overlay/preview.html
src/overlay/preview.tsx
build/*.AppImage

# Reddit promotion working notes (fact sheet, account notes, drafts)
.promote/

# Local GitHub traffic/download snapshots. Owner-only analytics, kept off the
# public repo. GitHub deletes traffic data after 14 days, so this folder is the
# only record of how the repo performed before each README/metadata change.
.metrics/

# Scratch space for bulk translation passes (see CONTRIBUTING_TRANSLATIONS.md)
.i18n-work/
